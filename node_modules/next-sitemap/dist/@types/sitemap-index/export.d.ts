import type { IRuntimePaths, IConfig } from '../interface';
/**
 * Export sitemap index file
 * @param runtimePaths
 * @param config
 * @returns
 */
export declare const exportSitemapIndex: (runtimePaths: IRuntimePaths, config: IConfig) => Promise<any>;
