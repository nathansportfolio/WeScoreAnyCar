import { IConfig, IRuntimePaths } from '../../interface';
/**
 * Export robots txt file
 * @param runtimePaths
 * @param config
 */
export declare const exportRobotsTxt: (runtimePaths: IRuntimePaths, config: IConfig) => Promise<any>;
